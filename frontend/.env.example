# Copy this file to .env.local (or .env) and set your deployment values.
# CRA only exposes variables prefixed with REACT_APP_.

REACT_APP_API_URL=http://localhost:8080/api
REACT_APP_BACKEND_URL=http://localhost:8080

